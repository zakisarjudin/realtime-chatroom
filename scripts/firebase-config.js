import { initializeApp } from "https://www.gstatic.com/firebasejs/9.17.2/firebase-app.js";

const firebaseConfig = {
  //set ur own config here
};

// Initialize Firebase
const app = initializeApp(firebaseConfig);

export { app };
